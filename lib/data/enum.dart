enum StorageMode {
  myFood, 
  saveFood, 
  recentView, 
  cookbook
}
enum ModeSelection {
  report,
  changePassword, 
  rating,
  settings
}
enum SettingsMode {
  about,
  appServices,
  theme,
  language
}