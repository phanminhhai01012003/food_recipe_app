enum StorageMode {
  myFood, 
  saveFood, 
  recentView, 
  cookbook,
  fridge
}
enum ModeSelection {
  report,
  changePassword, 
  rating,
  settings
}
enum SettingsMode {
  about,
  appServices,
  theme,
  language
}